import styled from "styled-components";

const StyledChatMessageInputContainer = styled.form`
  width: calc(100vw - 20rem);
  height: 10vh;
  position: fixed;
  bottom: 0;
`

export default StyledChatMessageInputContainer;